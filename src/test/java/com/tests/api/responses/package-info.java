/**
 * Typed response models deserialized from {@link com.tests.api.services}' calls - eventhub-specific
 * shapes (e.g. {@code EventResponse.availableSeats}), so they live alongside the services in
 * {@code src/test} rather than in {@code com.framework}.
 */
package com.tests.api.responses;
