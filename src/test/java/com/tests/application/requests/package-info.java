/**
 * Typed request payload models used by {@link com.tests.services} - eventhub-specific
 * shapes (e.g. {@code CreateEventRequest.totalSeats}), so they live alongside the services in
 * {@code src/test} rather than in {@code com.framework}.
 */
package com.tests.application.requests;
